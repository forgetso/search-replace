import { describe, test } from '@jest/globals'

describe('content background script', () => {
    test('combines iframe and non-iframe resposnes', () => {
        //expect(listenerContentResponse(1, 2)).toBe(3);
    })
})
